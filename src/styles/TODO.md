TODO



[x] Remember the camera preference and auto-start it on future visits. iOS still owns its permission prompt; setting Safari Website Settings → Camera → Allow prevents repeat prompts.

[x] Hide the camera viewing box after denied access or choosing keyboard play, and centre the selection panels.

[x] On mobile, dock the camera to the right and question bars to the left by default, with a setting to swap sides.

[x] On mobile, shrink an equation bar when its contents would overflow the screen.



Below are more complex. Leave for later
[  ] Introduce an alternate way to to shoot out the two bubbles. Instead of just pointing to the equation, you actually need to fire one to each side. Each side has a nice big hitbox though, and it just requires you passing over it rather than holding. I'd like to try this, so make it togglable to the original animated mode by pressing "s". By default, make it this new way though.
