TODO



[x] Can we please make it remember that I have given it camera access before on mobile browser? I'm playing on Safari / iPhone and I have to ask every time. (Done: Persisted camera state in localStorage 'algebra_camera_enabled'. On launch, the app auto-attempts to start the camera if previously enabled without showing the banner prompt. Added a "Remember Camera Access" toggle in settings and clear instructions for iOS Safari users on how to set Website Settings → Camera → Allow).

[ ] if I say no to camera access, can we get the camera viewing box to not show up? We could then centre the answers you select

[ ] On mobile, can we dock the camera to the RIGHT and the questions bars to the LEFT? And include a setting where I can toggle it. 



Below are more complex. Leave for later
[  ] Introduce an alternate way to to shoot out the two bubbles. Instead of just pointing to the equation, you actually need to fire one to each side. Each side has a nice big hitbox though, and it just requires you passing over it rather than holding. I'd like to try this, so make it togglable to the original animated mode by pressing "s". By default, make it this new way though.

